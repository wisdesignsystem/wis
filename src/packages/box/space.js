// export default function suitSpace(spaceElements, spaceRule) {
//   const [headerSpace, footerSpace] = spaceRule.split(/\n/).filter(Boolean)
//   const [infoSpaceName, operatorSpaceName] = headerSpace.split(/\s+/)
//   const [helpSpaceName, actionSpaceName] = footerSpace.split(/\s+/)

//   const [infoElement, operatorElement, helpElement, actionElement] = spaceElements

// }
